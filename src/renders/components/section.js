const PureComponent = require("./pureComponent");

module.exports = ({
  childrens,
  ableToContinue,
  title,
  subTitle,
  backgroundColor,
  titleColor
}) => PureComponent({
  childrens,
  ableToContinue,
  title,
  subTitle,
  backgroundColor,
  titleColor
}).render();